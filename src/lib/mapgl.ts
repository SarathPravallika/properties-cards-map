import {
  REACT_APP_MAPBOX_STYLE,
  REACT_APP_MAPBOX_TOKEN,
} from "../../constants";

const mapboxAccessToken = REACT_APP_MAPBOX_TOKEN;
const mapboxStyle = REACT_APP_MAPBOX_STYLE;

export { mapboxAccessToken, mapboxStyle };
